rules_version = '2';

// Firebase Storage: タスク完了報告画像（認証済みユーザーのみアップロード・読み取り可）
service firebase.storage {
  match /b/{bucket}/o {
    match /task_completion_images/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
